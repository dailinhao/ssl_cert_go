# 数据库配置
# 支持的类型: sqlite, mysql, postgresql

# SQLite 配置
type: sqlite
path: ./cert-manager.db

# MySQL 配置
# type: mysql
# host: localhost
# port: 3306
# user: root
# password: 123456
# database: cert_manager
